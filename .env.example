# Hugging Face (model download)
# HF_TOKEN=

# Reward (see train/tools/reward_swe.py)
# SWE_REWARD_MODE=dry_run|heuristic|subprocess
# SWE_REWARD_SCRIPT=/path/to/score.sh   # executable: args <instance_id> <prediction.txt> -> prints float
# SWE_REWARD_TIMEOUT=600
# SWE_REWARD_MODE=subprocess

# Training overrides for train/scripts/run_grpo_swe.sh
# VERL_ROOT=/path/to/repo_containing_verl_package
# MODEL_PATH=Qwen/Qwen2.5-7B-Instruct
# TRAIN_FILES=/path/to/train.parquet
# VAL_FILES=/path/to/val.parquet
# N_GPUS=4
# TOTAL_STEPS=1000
